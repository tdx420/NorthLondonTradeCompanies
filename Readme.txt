Develop a three-tiered software system with the following tiers:-
? A business tier, which should contain at least one core business object class (to represent a database record) and any associated logic.
? A data tier, which provides encapsulated access to the trade database and allows database modifications which should be persisted.
? A presentation tier, which provides a client/user graphical user interface using the Java Swing toolkit, and which should provide the